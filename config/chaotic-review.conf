[chaotic-review]
# "auto" selects the non-root owner of the pacman transaction terminal.
review_user = auto
state_dir = /var/lib/chaotic-review
gitlab_project = 54867625
gitlab_api = https://gitlab.com/api/v4
pacman_config = /etc/pacman.conf
